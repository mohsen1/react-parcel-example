import * as React from 'react';

export class Counter extends React.Component {
    public render() {
        return (
            <>
                Too lazy to count
            </>
        );
    }
}
